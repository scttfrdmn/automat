# Build output. Anchored: a bare "automat" would also match cmd/automat/.
/bin/
/dist/
/automat

# Test and coverage output
*.test
coverage.*

# gen/ scratch: fetched upstream sources are cached, never committed
/gen/.cache/

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/
