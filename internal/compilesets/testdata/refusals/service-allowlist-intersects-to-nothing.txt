cannot pack the merged control set into service control policies: the service allowlists
of the compiled control sets intersect to nothing, so no service would be permitted
(from 800-171r2:3.4.6, local-overlay:services). the control sets disagree about which
services may be used; compile a set whose service lists overlap, or override the service
allowlist explicitly
